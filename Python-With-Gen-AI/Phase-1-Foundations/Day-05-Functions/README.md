# Day 05 — Functions

Functions, parameters, return values, default arguments, scopes, and flexible arguments.

## Run a program

Open a terminal in this folder and run:

```powershell
python "<program-file>.py"
```

## Programs

- `args_kwargs.py` — Args Kwargs.
- `arguments_and_parameters.py` — Arguments And Parameters.
- `default_args.py` — Default Args.
- `defining_functions.py` — Defining Functions.
- `return_value.py` — Return Value.
- `scopes_var.py` — Scopes Var.

All examples are formatted with the project’s shared Python style.

