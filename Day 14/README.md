# Day 14

Embeddings, vector coordinates, and similarity comparison.

## Run a program

Open a terminal in this folder and run:

```powershell
python "<program-file>.py"
```

## Programs

- `compare.py` — Compare.
- `coordinates.py` — Coordinates.
- `first_embedding.py` — First Embedding.

All examples are formatted with the project’s shared Python style.

